<?xml version="1.0" encoding="utf-8"?>
<!--
    Per-app language preferences (Android 13+, Pixel 8/9).
    Declare supported locales so the system language picker works correctly.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
</locale-config>
